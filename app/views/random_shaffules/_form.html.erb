<%= form_with(model: random_shaffule, local: true) do |form| %>
  <% if random_shaffule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(random_shaffule.errors.count, "error") %> prohibited this random_shaffule from being saved:</h2>

      <ul>
      <% random_shaffule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question %>
    <%= form.text_area :question %>
  </div>

  <div class="field">
    <%= form.label :answer %>
    <%= form.text_area :answer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
